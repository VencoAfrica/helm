{{- if .Values.worker.amqp.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "erpnext.fullname" . }}-worker-amqp
  labels:
    {{- include "erpnext.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "erpnext.fullname" . }}-worker-amqp
  minReplicas: {{ .Values.worker.amqp.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.worker.amqp.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.worker.amqp.autoscaling.targetCPU }}
    - type: Resource
      resource:
        name: cpu
        {{- if semverCompare "<1.23-0" .Capabilities.KubeVersion.GitVersion }}
        targetAverageUtilization: {{ .Values.worker.amqp.autoscaling.targetCPU }}
        {{- else }}
        target:
          type: Utilization
          averageUtilization: {{ .Values.worker.amqp.autoscaling.targetCPU }}
        {{- end }}
    {{- end }}
    {{- if .Values.worker.amqp.autoscaling.targetMemory }}
    - type: Resource
      resource:
        name: memory
        {{- if semverCompare "<1.23-0" .Capabilities.KubeVersion.GitVersion }}
        targetAverageUtilization: {{ .Values.worker.amqp.autoscaling.targetMemory }}
        {{- else }}
        target:
          type: Utilization
          averageUtilization: {{ .Values.worker.amqp.autoscaling.targetMemory }}
        {{- end }}
    {{- end }}
{{- end }}
