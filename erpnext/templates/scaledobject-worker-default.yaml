{{- if .Values.worker.default.scaledObject.enabled }}
{{- if .Values.worker.default.autoscaling.enabled }}
{{- fail "worker.default: cannot enable both autoscaling and scaledObject. Set worker.default.autoscaling.enabled=false when worker.default.scaledObject.enabled=true." }}
{{- end }}
{{- if not .Values.worker.default.scaledObject.triggers }}
{{- fail "worker.default: scaledObject.triggers cannot be empty when scaledObject.enabled=true." }}
{{- end }}
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: {{ include "erpnext.fullname" . }}-worker-d
  labels:
    {{- include "erpnext.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "erpnext.fullname" . }}-worker-d
  minReplicaCount: {{ .Values.worker.default.scaledObject.minReplicaCount | default 1 }}
  maxReplicaCount: {{ required "worker.default.scaledObject.maxReplicaCount is required" .Values.worker.default.scaledObject.maxReplicaCount }}
  pollingInterval: {{ .Values.worker.default.scaledObject.pollingInterval | default 15 }}
  cooldownPeriod: {{ .Values.worker.default.scaledObject.cooldownPeriod | default 300 }}
  {{- with .Values.worker.default.scaledObject.advanced }}
  advanced:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.worker.default.scaledObject.fallback }}
  fallback:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  triggers:
    {{- toYaml .Values.worker.default.scaledObject.triggers | nindent 4 }}
{{- end }}